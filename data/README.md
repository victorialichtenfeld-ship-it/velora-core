This folder stores customer-validation records as JSONL:

- `leads.jsonl` — Get early access submissions
- `feedback.jsonl` — Pricing micro-survey
- `events.jsonl` — CTA clicks and scroll depth

Files are created on first submit. They are gitignored.
